# Sandbox Example

`sandbox.ts` is a deliberately flawed TypeScript file kept for smoke-testing the skill.

Use it to verify that the audit phase can identify:
- boundary violations,
- unsafe typing,
- validation gaps,
- silent failure handling,
- global state coupling,
- magic values and side-effect-heavy design.

The sandbox is illustrative only; it is not part of the skill runtime.
