# Forward Tests

This directory contains evaluation artifacts produced by exercising the skill against small, controlled inputs.

The goal is not to turn examples into source-of-truth instructions. The goal is to preserve concrete evidence that the skill can follow its own contracts.

Current artifact:
- `sandbox-audit-example.md`: audit-only forward test against `examples/sandbox/sandbox.ts`
